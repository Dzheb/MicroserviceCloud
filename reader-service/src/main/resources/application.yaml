server:
  port: 8380
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: reader-service
http:
  logging:
    enabled: true
    log-body: true
    log-level: warn
timing:
  enabled: true
  log-body: true
  log-level: warn